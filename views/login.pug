extends layout

block content
  h1 로그인

  form.login-form(action="/login" method="POST")
    input.login-input(type="text", name="id", placeholder="아이디를 입력하십시오.")
    input.login-input(type="password", name="pw", placeholder="비밀번호를 입력하십시오.")

    button.login-btn(type="submit") 로그인
